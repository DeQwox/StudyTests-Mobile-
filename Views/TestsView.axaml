<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:OnlineTestingClient.ViewModels"
             x:Class="OnlineTestingClient.Views.TestsView"
             x:DataType="vm:TestsViewModel">
  <Grid>
    <!-- <ListBox Items="{Binding Tests}" IsVisible="{Binding !IsLoading}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="{Binding Name}"/>
            <Button Command="{Binding DataContext.TakeTestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding Id}">Take Test</Button>
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox> -->
    <Panel IsVisible="{Binding IsLoading}">
      <!-- Loading spinner -->
    </Panel>
  </Grid>
</UserControl>